# Public API base URL. This is not a secret.
# Leave unset to use the deployed Cloud Run default embedded in the app.
VITE_API_BASE_URL=https://ciro-backend-nx552zjyba-el.a.run.app

# Use this only when running the backend locally.
# VITE_API_BASE_URL=http://127.0.0.1:8080
